{
  "name": "turbo-solid",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "dev:web": "turbo dev --filter=web",
    "build:web": "turbo build --filter=web",
    "web": "turbo dev --filter=web",
    "optimise": "turbo run optimise"
  },
  "devDependencies": {
    "prettier": "^3.6.2",
    "turbo": "^2.5.8",
    "typescript": "5.7.3"
  },
  "packageManager": "pnpm@10.12.1",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@crawl-me-maybe/web": "link:../../Library/pnpm/global/5/node_modules/@crawl-me-maybe/web"
  }
}
