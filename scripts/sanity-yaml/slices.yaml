ThisIsName:
  # title: string
  # date!4: datetime
  # itemsWithOptions[]: string(arrop1, arrop2) 
  # items: string(op1, op2, op3)
  # ref[]: ->stuff
  # docs[]: file(pdf,mdx)
  # description: text(4)
  # refArr[]: ->stuff
  # contact: email
  # count: number
  # itemList: string
  # points: geopoint
  # listItems: 
  #   - name: string
  #     count: number
  stuff: 
    you: string
    them: string
    listItems[]: 
      name: string
      other: number