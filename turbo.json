{
  "$schema": "https://turbo.build/schema.json",
  "ui": "tui",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": ["apps/web/.output/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "optimise": {
      "cache": false,
      "persistent": true
    }
  }
}
